import React from 'react';
import MainFooter from './MainFooter';

const Footer = () => {
    return (
        <div>
            <MainFooter></MainFooter>
        </div>
    );
};

export default Footer;